apiVersion: v1
kind: ConfigMap
metadata:
  name: consul-values
data:
  values.yaml: |
    global:
      nameOverride: consul
      fullnameOverride: consul

    server:
      enabled: true

    ui:
      enabled: true

    client:
      enabled: true

    connectInject:
      enabled: true
