apiVersion: v1
kind: Service # Tipo do objeto, no caso, um Service
metadata:
  name: consul
  namespace: consul
spec:
  selector: # Seleciona os Pods que serão expostos pelo Service
    app: consul # Neste caso, os Pods com o label app=my-app
  ports:
    - protocol: TCP
      port: 80 # Porta do Service      
      
