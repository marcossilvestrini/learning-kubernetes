apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kube-prometheus.fullname" . }}-test-connection"
  labels:
    {{- include "kube-prometheus.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kube-prometheus.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
